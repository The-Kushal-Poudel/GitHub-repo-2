# Portfolio Frontend

See `../README_FIRST.md` for setup instructions.

Local development automatically connects to the Laravel backend through `.env.development`. Production remains frontend-only when `VITE_API_URL` is not set.
