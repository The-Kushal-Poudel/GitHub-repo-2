# Optional. Set this only when you want the frontend/admin to use Laravel.
# Local development example:
VITE_API_URL=http://127.0.0.1:8000

# For a frontend-only Vercel deployment, do NOT set VITE_API_URL.
# The public portfolio will use src/data/portfolioData.js as its fallback data.
